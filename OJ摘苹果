#include <stdio.h>
int main(){
	
	int a[15];
	int high,cnt=0,i;
	
	for(i=0;i<10;i++){
		scanf("%d",a+i);
	}
	
	scanf("%d",&high);
	
	for(i=0;i<10;i++){
		if(a[i]<=high){
			cnt++;
		}else if(a[i]<=high+30){
			cnt++;
		}
		
	}
	printf("%d",cnt);
return 0;}
